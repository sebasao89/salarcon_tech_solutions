---
// Componente Hero rediseñado según la imagen de referencia
---

<section id="hero" class="relative min-h-screen flex items-center overflow-hidden bg-dark-500">
  <!-- Fondo con gradiente y efectos -->
  <div class="absolute inset-0 z-0">
    <!-- Gradiente de fondo -->
    <div class="absolute inset-0 bg-gradient-to-br from-dark-500 via-green-500/10 to-dark-500"></div>
    <!-- Patrón sutil -->
    <div class="absolute inset-0 opacity-20">
      <div class="absolute inset-0" style="background-image: radial-gradient(circle at 2px 2px, rgba(255,255,255,0.05) 1px, transparent 0); background-size: 60px 60px;"></div>
    </div>
    <!-- Overlay -->
    <div class="absolute inset-0 bg-dark-500/80"></div>
  </div>

  <!-- Contenido principal en grid -->
  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 w-full">
    <div class="grid lg:grid-cols-2 gap-8 lg:gap-12 items-center min-h-screen py-20">
      
      <!-- Columna izquierda: Contenido de texto -->
      <div class="space-y-8 lg:pr-8">
        <!-- Etiquetas superiores -->
        <div class="flex flex-wrap gap-3">
          <span class="geist-mono inline-block px-4 py-2 text-xs text-blue-500 bg-green-500/10 border border-green-500/20 rounded-full backdrop-blur-sm tracking-wider">
            SOLUCIONES EMPRESARIALES
          </span>
          <span class="geist-mono inline-block px-4 py-2 text-xs text-salmon-500 bg-salmon-500/10 border border-salmon-500/20 rounded-full backdrop-blur-sm tracking-wider">
            DOMÓTICA Y HOGAR INTELIGENTE
          </span>
        </div>

        <!-- Título principal -->
        <div class="space-y-6">
          <h1 class="work-sans text-3xl sm:text-4xl lg:text-5xl xl:text-6xl font-bold leading-tight">
            <span class="text-white-500">Innovación digital</span>
            <span class="text-salmon-500"> para empresas</span>
            <span class="text-blue-500"> y hogares</span>
          </h1>

          <!-- Descripción -->
          <p class="geist-mono text-base sm:text-lg text-white-500/80 leading-relaxed max-w-xl">
             transformamos la complejidad tecnológica en soluciones simples y poderosas, tanto para impulsar el crecimiento de tu empresa como para enriquecer la vida en tu hogar.
          </p>
        </div>

        <!-- Botones de acción -->
        <div class="flex flex-col sm:flex-row gap-4 pt-4">
          <a href="https://wa.me/573117827572?text=Hola,%20quiero%20comenzar%20un%20proyecto%20con%20ustedes.%20¿Podrían%20ayudarme?" target="_blank" rel="noopener noreferrer" class="geist-mono group relative px-8 py-4 bg-salmon-500 text-white-500 font-semibold rounded-lg transition-all duration-300 hover:bg-salmon-500/90 hover:scale-105 hover:shadow-lg hover:shadow-salmon-500/25 inline-block">
            <span class="relative z-10">Comenzar Proyecto</span>
            <div class="absolute inset-0 bg-gradient-to-r from-salmon-500 to-salmon-500/80 rounded-lg opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
          </a>
          
          <a href="#portfolio" class="geist-mono group px-8 py-4 border-2 border-blue-500 text-blue-500 font-semibold rounded-lg transition-all duration-300 hover:bg-blue-500 hover:text-dark-500 hover:scale-105 inline-block">
            Ver Portafolio
          </a>
        </div>
      </div>

      <!-- Columna derecha: Espacio para imagen/figura -->
      <div class="relative flex items-center justify-center lg:justify-end">
        <!-- Contenedor de la imagen/figura -->
        <div class="relative w-full max-w-lg lg:max-w-xl xl:max-w-2xl">
          <!-- Imagen principal circular -->
          <div class="relative aspect-square rounded-full overflow-hidden backdrop-blur-sm border border-white-500/10">
            <!-- Efectos de fondo para la imagen -->
            <div class="absolute inset-0 bg-gradient-to-tr from-blue-500/10 to-transparent rounded-full"></div>
            <div class="absolute inset-0 bg-gradient-to-br from-green-500/10 via-transparent to-salmon-500/10 rounded-full"></div>
            
            <!-- Imagen principal -->
            <div class="absolute inset-0 flex items-center justify-center p-4">
              <img 
                src="/logo/img-home.png" 
                alt="Salarcon Tech Solutions - Innovación Digital" 
                class="w-full h-full object-cover rounded-full"
              />
            </div>
          </div>

          <!-- Elementos decorativos alrededor de la imagen -->
          <div class="absolute -top-4 -right-4 w-8 h-8 bg-salmon-500/60 rounded-full animate-pulse"></div>
          <div class="absolute -bottom-6 -left-6 w-6 h-6 bg-blue-500/60 rounded-full animate-pulse" style="animation-delay: 1s;"></div>
          <div class="absolute top-1/4 -left-8 w-4 h-4 bg-green-500/60 rounded-full animate-pulse" style="animation-delay: 2s;"></div>
          <div class="absolute bottom-1/3 -right-8 w-3 h-3 bg-salmon-500/40 rounded-full animate-pulse" style="animation-delay: 0.5s;"></div>
        </div>
      </div>
    </div>
  </div>

  <!-- Indicador de scroll -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 z-20">
    <div class="flex flex-col items-center text-white-500/60">
      <span class="geist-mono text-xs mb-2 tracking-wider">SCROLL</span>
      <div class="w-px h-12 bg-gradient-to-b from-white-500/60 to-transparent animate-pulse"></div>
    </div>
  </div>

  <!-- Elementos decorativos de fondo -->
  <div class="absolute top-1/4 left-10 w-2 h-2 bg-salmon-500/40 rounded-full opacity-60 animate-pulse"></div>
  <div class="absolute top-1/3 right-20 w-1 h-1 bg-blue-500/60 rounded-full opacity-80 animate-pulse" style="animation-delay: 1s;"></div>
  <div class="absolute bottom-1/4 left-1/4 w-1.5 h-1.5 bg-green-500/50 rounded-full opacity-70 animate-pulse" style="animation-delay: 2s;"></div>
</section>

<style>
  /* Animaciones personalizadas */
  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
  }
  
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }

  /* Efecto de respiración para el placeholder de imagen */
  @keyframes breathe {
    0%, 100% { transform: scale(1); opacity: 0.8; }
    50% { transform: scale(1.05); opacity: 1; }
  }
  
  .animate-breathe {
    animation: breathe 4s ease-in-out infinite;
  }
</style>

